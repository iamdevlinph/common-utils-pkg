codecov:
  strict_yaml_branch: main  # only use the latest copy on master branch

coverage:
  precision: 2
  round: down
  range: "70...100"
  status:
    patch: off
    project:
      default:
        # basic
        target: 70%
