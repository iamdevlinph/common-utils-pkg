/**
 * Removes all data stored in the local storage.
 *
 * @module Storage
 * @name storageClear
 * @example
 *
 * storageClear();
 *
 */
export const storageClear = () => window.localStorage.clear();
