/**
 * Removes all data stored in the local storage.
 *
 * @module StorageUtil
 * @name clearAll
 * @example
 *
 * StorageUtil.clearAll();
 *
 */
const clearAll = () => window.location.clear();

export default clearAll;
